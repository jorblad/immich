apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-nfs-pvc
spec:
  volumeName: immich-nfs-pv
  storageClassName: ""
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1000Gi
